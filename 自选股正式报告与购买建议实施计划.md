# 实施计划：自选股正式研究报告与个股购买建议

> 请 GPT-5.6 Sol 按照本计划直接修改、测试并重新构建 Docker。不要修改历史运行记录，也不要绕过数据、风控和 A 股交易规则。

## 一、目标

调整“我的自选与持仓”定向研究逻辑：

1. 自选股不足 15 只时，不能因为数量不足而将整个研究任务标记为 `FUSED`。
2. 每只自选股都必须出现在正式研究报告中。
3. 数据完整、通过规则和风险门禁的股票，可以生成单股购买建议。
4. 数据不完整或风险过高的股票，也要生成正式研究结论，但固定为 `NO_BUY`，并显示明确原因。
5. 组合仓位方案继续执行至少 15 只、行业集中度、回撤熔断等约束。
6. 不把原有 `FUSED` 记录修改为 `SUCCEEDED`；修改只影响后续新运行。

## 二、业务状态定义

| 场景 | 研究运行状态 | 个股报告 | 个股购买建议 | 组合方案 |
|---|---|---|---|---|
| 自选少于 15 只，数据完整 | `SUCCEEDED` | 正式报告 | 可以生成 | 不生成 |
| 自选不少于 15 只，组合条件满足 | `SUCCEEDED` | 正式报告 | 可以生成 | 可以生成 |
| 部分股票数据不完整 | `SUCCEEDED` | 所有股票都有报告 | 完整股票可生成；不完整股票固定 `NO_BUY` | 仅使用正式合格股票判断 |
| 正式合格股票不足 15 只 | `SUCCEEDED` | 正式报告 | 合格股票可生成 | 不生成 |
| 最大回撤等系统级熔断 | `FUSED` | 观察报告 | 全部禁止 | 禁止 |
| 流水线异常 | `FAILED` | 不保证生成 | 禁止 | 禁止 |

`FUSED` 只用于系统级或风险级熔断，例如：

- `MAX_DRAWDOWN_FUSE_ACTIVE`；
- 时点污染或未来数据；
- 关键交易规则缺失；
- 无法构造可信的不可变快照；
- 其他必须全局停止购买建议的风险。

`INSUFFICIENT_COMPLETE_SYMBOLS` 不再导致 `WATCHLIST` 或 `CUSTOM` 研究变成 `FUSED`，而应成为组合未生成的原因。

## 三、现有问题定位

重点检查以下入口：

- `src/ashare_ai/orchestration/builtin.py`
  - `risk_state()`
  - `_formal_eligibility()`
  - `_cumulative_backtest_signals()`
  - `portfolio_requested()`
  - `publish_report()`
- `src/ashare_ai/orchestration/daily.py`
- `src/ashare_ai/orchestration/research_jobs.py`
- `src/ashare_ai/api/app.py`
  - `submit_research()`
  - `submit_trade_plan()`
  - 报告、候选和组合查询接口
- `src/ashare_ai/api/schemas.py`
- `src/ashare_ai/reports/daily.py`
- `src/ashare_ai/reports/templates/daily.html.j2`
- `web/src/pages/ResearchPage.tsx`
- `web/src/pages/ReportsPage.tsx`
- `web/src/pages/PortfolioPage.tsx`
- `web/src/api.ts`
- `web/src/types.ts`

当前代码已经存在：

```python
portfolio_requested = payload.scope == "MARKET" or len(target_symbols) >= 15
```

但 `risk_state()` 又会因为正式合格股票少于 `target_count`，将运行切换为 `OBSERVE_ONLY/FUSED`。需要修复这两个逻辑之间的冲突。

同时移除 API 层散落的硬编码 `15`，从版本化策略配置读取 `portfolio.target_count`，不要在路由中复制业务规则。

## 四、后端改造

### 1. 分离研究状态、数据资格和组合状态

不要再用一个 `risk_state` 同时表示：

- 全局风控状态；
- 个股数据完整性；
- 能否构建组合。

建议在 Manifest 中明确保存：

```json
{
  "research_scope": "WATCHLIST",
  "research_outcome": {
    "status": "SUCCEEDED"
  },
  "data_quality_gate": {
    "formal_eligible_symbols": [],
    "excluded_symbols": {}
  },
  "portfolio_outcome": {
    "requested": false,
    "generated": false,
    "reason_code": "INSUFFICIENT_DIVERSIFICATION",
    "reason_message": "正式合格股票不足组合要求，但不影响个股正式研究"
  },
  "risk_outcome": {
    "state": "NORMAL",
    "reason_code": null
  }
}
```

### 2. 修改 `risk_state()`

- 最大回撤等全局风控继续返回 `OBSERVE_ONLY`。
- 对 `WATCHLIST`、`CUSTOM` 定向研究：
  - 正式合格数量不足 `target_count` 时，不得切换成 `OBSERVE_ONLY`；
  - 保存每只股票的数据排除原因；
  - 在 `portfolio_outcome` 中记录组合不可生成；
  - 运行最终状态保持 `SUCCEEDED`。
- `MARKET` 研究原有严格组合门禁可以保留，避免无意改变每日市场组合策略。

### 3. 保留全部目标股票的研究结果

报告必须覆盖：

- `target_symbols` 中的全部股票；
- 每只股票的评分数据；
- 是否正式合格；
- 排除原因码；
- 数据质量摘要；
- 是否允许生成购买建议。

不要把数据不完整股票伪装成正式候选。可以新增从 Manifest 和 `ScoreRow` 派生的报告股票响应，例如：

```json
{
  "symbol": "600000.SH",
  "research_status": "FORMAL",
  "advice_eligible": true,
  "recommendation": null,
  "exclusion_reasons": []
}
```

数据不足时：

```json
{
  "symbol": "600001.SH",
  "research_status": "FORMAL_WITH_LIMITATIONS",
  "advice_eligible": false,
  "recommendation": "NO_BUY",
  "exclusion_reasons": ["MISSING_OFFICIAL_DISCLOSURE"]
}
```

优先从现有表和 Manifest 派生。除非确有持久化必要，否则不要增加数据库表。

### 4. 个股购买建议

`submit_trade_plan()` 不应再因为“自选数量不足 15 只”而拒绝单股建议，但必须继续检查：

- 运行状态必须为 `SUCCEEDED`；
- 股票属于本次研究的目标股票；
- 股票通过个股数据完整性门禁；
- `event_risk_multiplier > 0`；
- 不处于全局回撤熔断；
- 使用已提交且可校验哈希的历史验证快照；
- 具有足够训练期、验证期和未来交易日；
- A 股交易规则、手数、涨跌停、停牌、T+1 等校验完整。

不得通过删除快照检查或降低时点校验来实现购买建议。

不满足条件时返回结构化错误码，例如：

- `SYMBOL_DATA_INCOMPLETE`
- `CRITICAL_EVENT_RISK`
- `GLOBAL_RISK_FUSE_ACTIVE`
- `INSUFFICIENT_VALIDATION_HISTORY`
- `TRADING_RULE_UNAVAILABLE`

前端据此展示 `NO_BUY` 和中文原因。

### 5. 研究专用快照

检查当前“小规模定向研究不构建组合”时，是否仍会产生个股购买建议所需的已提交验证快照。

如果不会：

- 为通过资格的目标股票生成个股建议验证所需的不可变快照；
- 不得把单股信号伪装成满足 15 只约束的组合信号；
- 在快照详情中区分用途，例如 `SINGLE_SYMBOL_ADVICE` 和 `PORTFOLIO_BACKTEST`；
- 调整交易建议 Worker，使其选择正确用途的快照；
- 保留全部 SHA-256、`decision_at`、`available_at` 和 Manifest 校验。

## 五、正式报告改造

日报正文必须覆盖全部自选股，每只股票至少展示：

- 股票代码和名称；
- 最终分与各维度评分；
- 数据截止时间；
- 证据和数据来源；
- 基本面、技术面、情绪面结论；
- 数据完整性；
- 风险等级；
- 是否允许生成购买建议；
- `BUY`、`NO_BUY` 或“尚未生成建议”；
- 限制原因和风险提示。

报告中明确说明：

> 本报告是正式研究报告；是否允许生成交易建议，由个股数据门禁和全局风控分别决定。

数据不完整的股票仍进入报告，但不能输出虚构的买入价格、仓位或止损位。

## 六、前端改造

### 1. 研究中心

对于自选研究：

- 显示“将为 N 只自选/持仓股票生成正式研究报告”；
- 少于 15 只时提示：个股正式报告正常生成，但因分散度不足不生成整体组合；
- 不再显示为“观察模式/FUSED”。

### 2. 报告页面

将“正式候选个股工作台”调整为“自选股研究工作台”：

- 展示本次目标股票中的全部股票；
- 使用标签区分 `正式可建议`、`数据受限`、`风险禁买`；
- 正式合格股票显示“生成购买建议”按钮；
- 数据不完整或重大风险股票禁用按钮，并直接显示 `NO_BUY` 原因；
- 只有真正 `FUSED` 时才显示“全局风控熔断，禁止生成交易方案”。

### 3. 组合页面

小规模自选研究显示：

- 状态：`SUCCEEDED`；
- 文案：正式个股研究已完成；
- 组合状态：未生成；
- 原因：股票数量或正式合格数量不足组合分散度要求。

不得显示成任务失败或观察模式。

### 4. 移动端

- 股票标签允许横向滚动或紧凑换行；
- 评分卡在手机上单列或双列；
- 操作按钮使用足够触控高度；
- 表格在窄屏下使用卡片化或横向滚动；
- 不出现页面级横向溢出。

## 七、测试要求

### 1. 后端测试

新增或修改以下场景：

1. `WATCHLIST` 只有 1 只且数据完整：
   - 状态为 `SUCCEEDED`；
   - 有正式报告；
   - 无组合；
   - 可以提交该股购买建议。
2. `WATCHLIST` 有 3 只，其中 1 只数据不完整：
   - 状态为 `SUCCEEDED`；
   - 报告包含 3 只；
   - 2 只可生成建议；
   - 不完整股票固定 `NO_BUY`；
   - 无组合。
3. `WATCHLIST` 不足 15 只：
   - `INSUFFICIENT_COMPLETE_SYMBOLS` 不得造成 `FUSED`。
4. 最大回撤熔断：
   - 状态仍为 `FUSED`；
   - 所有购买建议均被拒绝。
5. 满足 15 只及全部组合约束：
   - 原有组合生成行为不变。
6. 购买建议继续拒绝：
   - 未来数据；
   - 未提交快照；
   - 数据不完整股票；
   - CRITICAL 风险股票；
   - 不属于本次报告的股票。

重点检查：

- `tests/unit/test_builtin_pipeline.py`
- `tests/unit/test_research_jobs.py`
- `tests/unit/test_trade_plan_api.py`
- `tests/unit/test_api_audit.py`
- `tests/unit/test_reports.py`
- `tests/unit/test_web_auth_market.py`

### 2. 前端测试

覆盖：

- 小规模自选研究显示“已完成”，而不是 `FUSED`；
- 报告页展示全部自选股；
- 合格股票可点击生成建议；
- 不合格股票显示 `NO_BUY` 和原因；
- 组合页显示“研究已完成、组合未生成”；
- 桌面和手机布局无横向溢出。

重点检查：

- `web/src/test/research-backtest.test.tsx`
- 新增或扩展报告工作台测试。

## 八、兼容与安全要求

- 不修改已有历史 `FUSED` 记录；
- 不修改既有 Alembic migration；
- 若无需新字段，优先使用 Manifest 扩展，避免数据库迁移；
- 不改变已版本化配置的既有含义；
- 不绕过 `decision_at`、`available_at` 和 PIT 校验；
- 不降低交易规则、数据完整性和回测验证要求；
- 保留当前工作区已有未提交改动，不覆盖或回滚其他人的修改。

## 九、验证命令

后端测试：

```powershell
.\.venv\Scripts\python -m pytest tests/unit/test_builtin_pipeline.py tests/unit/test_research_jobs.py tests/unit/test_trade_plan_api.py tests/unit/test_reports.py tests/unit/test_web_auth_market.py
```

前端测试与构建：

```powershell
cd web
npm test -- --run
npm run build
```

执行相应范围的 Ruff 与 mypy。

按照 `AGENTS.md`，代码更新完成后必须重新构建并部署到本机 Docker Desktop：

```powershell
docker compose up -d --build
docker compose ps
```

最后检查：

- Web：`http://localhost`；
- API 健康接口；
- Research Worker；
- Backtest Worker；
- 容器无持续重启或关键错误日志。

如果中文路径导致 Docker BuildKit 出现 gRPC header 编码错误，可以临时将工作区映射到纯 ASCII 盘符后构建，但构建完成后必须恢复真实路径挂载，并确认 `minio-init` 没有保留临时盘符绑定。

## 十、完成标准

只有同时满足以下条件才算完成：

- 1 只自选股也能生成正式报告；
- 自选股全部出现在报告中；
- 数据合格股票可以生成个股购买建议；
- 不合格股票明确显示 `NO_BUY`；
- 自选数量不足 15 只不会触发 `FUSED`；
- 组合风控和最大回撤熔断没有被绕过；
- 自动化测试、前端构建和 Docker Desktop 重建全部通过。
